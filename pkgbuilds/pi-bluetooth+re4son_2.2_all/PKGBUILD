# Generated by debtap
# Maintainer: 3xitlight
# Contributor: 3xitlight
pkgname=pi-bluetooth+re4son-bin
pkgver=2.2
pkgrel=1
pkgdesc="Bluetooth services and rules for Raspberry Pi. This package contains services and udev rules required for Bluez to work with the Broadcom bluetooth chip found in the Raspberry Pi 3 & Zero W"
arch=('armv6h')
url="https://www.whitedome.com.au/re4son/re4son-kernel"
license=('')
groups=('')
groups=('')
depends=('bluez-firmware' 'bluez-utils>=5.43')
provides=('pi-bluetooth')
replaces=('pi-bluetooth')
backup=('etc/udev/rules.d/99-com.rules')
options=('!strip' '!emptydirs')
install=${pkgname}.install
source=("PUT_FULL_URL_FOR_DOWNLOADING_DEB_PACKAGE_HERE")
sha512sums=('9a26d5808f3b0243b4053d5d3d965f640691ceb96cc749934e0505e0da9ecab46397ad6f2a7f6cd7d7635c4e77a33262f27dea6e65302afe29eef1518de6cd8b')

package(){

	# Extract package data
	tar xf data.tar.xz -C "${pkgdir}"

	# Fix directories structure differencies
	cd "${pkgdir}"

	#install -D -m644 "/usr/share/doc/pi-bluetooth+re4son/copyright" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
	mkdir -p usr/lib 2> /dev/null; mv lib/* usr/lib; rm -rf lib

	cd ..

}
