# Generated by debtap
# Maintainer: 3xitlight
# Contributor: 3xitlight
pkgname=pi-bluetooth-bin
pkgver=0.1.4.re4son
pkgrel=1
pkgdesc="Raspberry Pi 3 bluetooth. Loads BCM43430A1 firmware on boot Restarts on error - Re4son"
arch=('armv6h')
url="https://github.com/RPi-Distro/rpi-bluetooth"
license=('')
groups=('')
depends=('bash-completion>=1.18.' 'bluez-firmware' 'bluez-utils')
options=('!strip' '!emptydirs')
install=${pkgname}.install
source=("PUT_FULL_URL_FOR_DOWNLOADING_DEB_PACKAGE_HERE")
sha512sums=('37ac8c8d3576952f763188547dd452c54db16a24e8cba24c7910c1668a2c6ac89cc1333369261964971ddace62200dc1ccc1b62783a91fd41ca1c5bd49137252')

package(){

	# Extract package data
	tar xf data.tar.xz -C "${pkgdir}"

	# Fix directories structure differencies
	cd "${pkgdir}"

	#install -D -m644 "/usr/share/doc/pi-bluetooth/copyright" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
	mkdir -p usr/lib 2> /dev/null; mv lib/* usr/lib; rm -rf lib

	cd ..

}
